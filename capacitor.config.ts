import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reign.lemure',
  appName: 'lemure',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
